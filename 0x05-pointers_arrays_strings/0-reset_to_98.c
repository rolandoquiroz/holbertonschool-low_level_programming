#include "holberton.h"
/**
 * reset_to_98 - to an int as parameter and updates the val it points to to 98
 *
 * @*n: int *n
 *
 * Return: void
 */
void reset_to_98(int *n)
{
int ret;
if (c > 64 && c < 91)
{
ret = 1;
}
else
{
ret = 0;
}
return (ret);
}
