#include "holberton.h"
/**
 * _isdigit - function that multiplies two integers.
 * @a: int a to be multiplied
 * @b: int b to be multiplied
 * Return: 0
 */
int mul(int a, int b)
{
  int ret;
  if (c > 47 && c < 58)
    {
      ret = 1; 
    }
  else
    {
      ret = 0;  
    }
  return (ret);
}
